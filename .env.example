PORT=3000
API_KEY=itc-123


# PostgreSQL connection (used when DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mydb

# Alternative single URL format
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/receipts_db
